local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Safely Fetch Remotes
local PlaceEvent = ReplicatedStorage:FindFirstChild("PlaceTurret") or ReplicatedStorage:WaitForChild("PlaceTurret", 3)
local ActionEvent = ReplicatedStorage:FindFirstChild("TurretAction") or ReplicatedStorage:WaitForChild("TurretAction", 3)
local SpeedEvent = ReplicatedStorage:FindFirstChild("SetGameSpeed") or ReplicatedStorage:WaitForChild("SetGameSpeed", 3)

-- File Storage Compatibility Check
local FOLDER_NAME = "TowerMacroConfig"
local isFileSystemSupported = (writefile and readfile and isfile) and true or false

if isFileSystemSupported and makefolder and not isfolder(FOLDER_NAME) then
	pcall(function() makefolder(FOLDER_NAME) end)
end

-- State Variables
local isRecording = false
local isPlaying = false
local autoSpeedEnabled = true
local autoPlayEnabled = false
local startTime = 0
local macroData = {}

local recordedTurretsOrder = {}
local spawnedTurretsPlayback = {}

-- Helper Functions
local function setSpeed(multiplier)
	if SpeedEvent then
		if SpeedEvent:IsA("RemoteFunction") then
			pcall(function() SpeedEvent:InvokeServer(multiplier) end)
		else
			pcall(function() SpeedEvent:FireServer(multiplier) end)
		end
	end
end

-- ==========================================
-- UNIVERSAL DRAGGABLE FUNCTION (PC + Mobile)
-- ==========================================
local function makeUniversalDraggable(guiObject)
	local dragging, dragInput, dragStart, startPos

	local function update(input)
		local delta = input.Position - dragStart
		guiObject.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end

	guiObject.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = guiObject.Position

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	guiObject.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			update(input)
		end
	end)
end

-- ==========================================
-- RESPONSIVE UI SETUP (All Devices)
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TowerMacroUI_Universal"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 100
ScreenGui.Parent = PlayerGui

-- Panel Utama
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 220, 0, 290)
MainFrame.Position = UDim2.new(0.05, 0, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)
makeUniversalDraggable(MainFrame)

-- Title Bar
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0.6, 0, 0, 26)
Title.Position = UDim2.new(0.04, 0, 0, 0)
Title.Text = "Macro Manager"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 11
Title.Font = Enum.Font.SourceSansBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.BackgroundTransparency = 1
Title.Parent = MainFrame

-- Minimize Button
local BtnMinimize = Instance.new("TextButton")
BtnMinimize.Size = UDim2.new(0, 22, 0, 22)
BtnMinimize.Position = UDim2.new(1, -50, 0, 2)
BtnMinimize.Text = "-"
BtnMinimize.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
BtnMinimize.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnMinimize.Font = Enum.Font.SourceSansBold
BtnMinimize.TextSize = 14
BtnMinimize.Parent = MainFrame
Instance.new("UICorner", BtnMinimize).CornerRadius = UDim.new(0, 4)

-- Close Button
local BtnClose = Instance.new("TextButton")
BtnClose.Size = UDim2.new(0, 22, 0, 22)
BtnClose.Position = UDim2.new(1, -25, 0, 2)
BtnClose.Text = "X"
BtnClose.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
BtnClose.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnClose.Font = Enum.Font.SourceSansBold
BtnClose.TextSize = 12
BtnClose.Parent = MainFrame
Instance.new("UICorner", BtnClose).CornerRadius = UDim.new(0, 4)

-- Restore Floating Button
local RestoreBtn = Instance.new("TextButton")
RestoreBtn.Size = UDim2.new(0, 80, 0, 30)
RestoreBtn.Position = UDim2.new(0.05, 0, 0.2, 0)
RestoreBtn.Text = "Open Macro"
RestoreBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
RestoreBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
RestoreBtn.Font = Enum.Font.SourceSansBold
RestoreBtn.TextSize = 11
RestoreBtn.V
